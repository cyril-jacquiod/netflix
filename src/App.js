import "./App.css";
import Section from "./components/Section";

const Section = () => {
  return (
    <div>
      {data.map((item) => {
        console.log(item);
      })}
    </div>
  );
};

export default Section;
